<header>
  <div>
        <h1>LOL GUIDE</h1>
        <h4>Guides et Actus League of Legend</h4>
        <nav>
            <ul class="nav nav-pills">
                <li role="presentation">
                    <a href="{{ path('post') }}">Acceuil</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li role="" class="nav_admin">
                        <a href="{{ path('fos_user_security_logout') }}">Logout</a>
                    </li>
                    <li role="" class="nav_admin">
                        <a href="{{ path('post_create') }}">Poster</a>
                    </li>
                {% else %}
                    <li role="presentation">
                        <a href="{{ path('fos_user_security_login') }}">Sign in</a>
                    </li>
                    <li role="presentation">
                        <a href="{{ path('fos_user_registration_register') }}">Sign up</a>
                    </li>
                {% endif %}

            </ul>
        </nav>
      </div>
</header>
